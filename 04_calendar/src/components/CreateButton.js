import React from 'react';
import './scss/CreateButton.scss';

class CreateButton extends React.Component {
  render() {
    return (
      <button className="create-button">Create</button>
    );
  }
}

export default CreateButton;