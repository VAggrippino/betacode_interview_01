/*******************************************************************************
 * 
 * 3. Write a react form to update user profile.
 *     - Fields: first name, last name, email, birth date, preferred job title,
 *       number of years experience
 *     - send data to dummy end point https://api.dummyendpoint/me/profile with
 *       http PUT
 *     - validate fields name and email to be required
 * 
 */

import React from 'react';
import ReactDOM from 'react-dom';
import App from './components/App';

ReactDOM.render( <App />, document.getElementById('root') );